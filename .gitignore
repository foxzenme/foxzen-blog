### Python
# Byte-compiled files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# dotenv environment variable files
.env

# Virtual environments
.venv
env/
venv/

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.pytest_cache/

# Type checkers
.mypy_cache/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
# .python-version

### Windows
# Windows thumbnail cache files
Thumbs.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows shortcuts
*.lnk

### 项目运行时产物（不应提交，见 zip_cache.py / generate_status_page.py 的说明）
data/zip_cache/
static_status/

# Production database and SQLite runtime files
data/blog.db
data/blog.db-shm
data/blog.db-wal

# Local secrets / credentials
.env
*.env
*.pem
*.key
*.secret
*.token

# Pages发布构建产物(由publish_build.py/build_status_page.py现场生成，不需要进Git)
/publish/
/publish_status/

# 图片（仓库根目录等处的杂项图片不需要进Git）
*.png
# S1修复：上面这条全局规则会连带吞掉html/发布目录下博客正文的图片
# （html/posts/<id>/media/*.png、html/images/*.png等），导致git_publish.py
# 的`git add -- html`永远看不到新增图片，github/cf发布出来的文章会缺图，
# 却仍然报告发布成功——html/是实际发布内容，不是"杂项图片"这条规则原本
# 想排除的对象，必须排除在外。
!html/**/*.png